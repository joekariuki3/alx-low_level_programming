#include"main.h"

/**
 * _abs - print absolute value of a number
 *
 *@int: is requred as input
 *
 * Return: 0 sucess
 */
int _abs(int);
{
	int i
	if (i < 0)
	{
		i = -i;
		_putchar(i);
	}
	else
	{
		_putchar(i);
	}
	return (0);
}
