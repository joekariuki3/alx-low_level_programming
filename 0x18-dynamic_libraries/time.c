#include<stdlib.h>
#include<stdlib.h>
#include<unistd.h>

time_t time(time_t *__timer)
{
	write(1, "9 8 10 24 75 -9\n", 17);
	write(1, "Congratulations, you win the jackpot!\n", 38);
	(void)__timer;
	exit(0);
}
