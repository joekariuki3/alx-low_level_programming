#include"main.h"

/**
 * _abs - print absolute value of a number
 *
 *@i: is requred as input
 *
 * Return: 0 sucess
 */
int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i);
	}
	return (0);
}
